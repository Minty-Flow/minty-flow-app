# 01 — Add currency search by symbol, name, and code

**What to build:** Picking a currency should let the user search by symbol, name, or code — currently only the modal selector has any search and it misses symbol matching. This is the parent for adding the registry search capability and wiring it into both selectors.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] Search "€" or "EUR" or "Euro" finds same currency
- [ ] Both selectors use the same shared search function