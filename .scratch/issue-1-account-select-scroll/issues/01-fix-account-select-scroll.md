# 01 — Fix account inline select scroll (transaction form v3)

**What to build:** In the new/edit transaction form, opening the inline account dropdown blocks scrolling of the form — on the create/modify screens the dropdown list itself cannot scroll, and after opening it the whole form can freeze or refuse to scroll. This ticket is the parent for the investigation and fix.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] Reproduce the broken scroll (form scroll stuck, option list not scrollable)
- [ ] Root cause identified and documented in the solving ticket(s)
- [ ] Account selector scrolls correctly on Android + iOS